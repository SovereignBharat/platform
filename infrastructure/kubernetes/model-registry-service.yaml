apiVersion: v1
kind: Service
metadata:
  name: model-registry
  namespace: sovereignbharat
spec:
  selector:
    app: model-registry
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8084
